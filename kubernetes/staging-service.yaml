apiVersion: v1
kind: Service
metadata:
  name: devops-platform-service
  namespace: staging
spec:
  selector:
    app: devops-platform
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
  type: NodePort